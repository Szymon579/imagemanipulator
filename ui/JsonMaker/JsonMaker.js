function export2json(r, g, b) {

    let originalData =
            {
                "red": r,
                "green": g,
                "blue": b
            }

    //JSON.stringify(originalData);
    console.log(originalData);

}
